import { Box, Chip, Stack } from '@mui/material';

const SearchSection = () => {
	return (
		<Stack direction='row'>
			<Box>
				<Chip label='Clickable Deletable' />
			</Box>
		</Stack>
	);
};

export default SearchSection;
